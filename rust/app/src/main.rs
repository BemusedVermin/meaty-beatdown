//! The Bevy game shell — Phase 7. Until then this stub exists to enforce the one-way
//! `app -> engine` dependency arrow from day one (tech-plan §1).

fn main() {
    println!("TICK app stub — engine v{}", engine::version());
}
