//! The combat sim. Phase 0 holds only the advance-loop skeleton (`sim`); the full module
//! map (entity, schedule, spatial, resolve, observe, agents — tech-plan §3) lands across
//! Phases 1–5.

pub mod sim;
