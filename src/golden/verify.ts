// golden/verify.ts — loads each vector, replays via ReplayAgent, deep-equals the trace [edge].
// STUB: implemented in Phase 8. Wired into `npm test` as the cross-language behavioral contract.
export {};
