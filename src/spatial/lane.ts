// spatial/lane.ts — pos (lane) + offset (sidestep) + height as Fixed; doesHit w/ tracking [L1].
// All contact math lives here behind the single doesHit predicate (audit C-7).
// STUB: implemented in Phase 2. See spec §1.1, §1.2.
export {};
