# Normalize all text to LF in the repo and on checkout. The golden-vector format requires LF
# newlines (Phase 8), and the determinism contract wants byte-stable files across platforms.
* text=auto eol=lf

# Binary — never normalize.
*.png binary
*.jpg binary
